use std::collections::HashMap;

use diploma_algos::{ load_dataset, use_this_library_in_main};
// ─── main ───────────────────────────────────────────────────────────────────

fn main() -> Result<(), Box<dyn std::error::Error>> {
    use_this_library_in_main()
}
